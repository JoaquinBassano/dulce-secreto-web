---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div
  class={`overflow-hidden border-2 border-brand-secondary bg-white shadow-lg transition-shadow hover:shadow-xl rounded-lg ${className}`}
>
  <slot />
</div>
